# دریافت پروژه‌های کارلنسر - نسخه لینوکس

این برنامه پروژه‌های جدید را از کارلنسر دریافت و ذخیره می‌کند، با پشتیبانی کامل از زبان فارسی در لینوکس.

## مشکلاتی که حل شد

### مشکل در ویندوز vs لینوکس
کد قبلی در ویندوز کار می‌کرد اما در لینوکس متن‌های فارسی را درست نمایش نمی‌داد. دلایل اصلی:

1. **Locale نامناسب**: سیستم‌های لینوکس به طور پیش‌فرض ممکن است locale فارسی نداشته باشند
2. **Encoding در stdout**: terminal لینوکس ممکن است UTF-8 نباشد
3. **Environment variables**: متغیرهای محیطی مناسب تنظیم نشده بودند
4. **File encoding**: همه فایل‌ها باید صریحاً با UTF-8 باز شوند

### راه‌حل‌های پیاده‌سازی شده

✅ **تنظیم خودکار locale** به `C.UTF-8` یا `en_US.UTF-8`
✅ **Reconfigure کردن stdout/stderr** به UTF-8
✅ **تنظیم environment variables**: `PYTHONIOENCODING`, `LANG`, `LC_ALL`
✅ **استفاده صریح از encoding='utf-8'** در تمام عملیات فایل
✅ **Error handling بهتر** با پیام‌های فارسی
✅ **اسکریپت راه‌انداز** برای تنظیم خودکار محیط

## نصب و راه‌اندازی

### روش ۱: استفاده از اسکریپت خودکار (توصیه می‌شود)

```bash
# اجرا با یک دستور
./run_fetcher.sh
```

این اسکریپت:
- تمام متغیرهای محیطی را تنظیم می‌کند
- کتابخانه‌های لازم را نصب می‌کند
- برنامه را با تنظیمات صحیح اجرا می‌کند

### روش ۲: نصب دستی

```bash
# نصب کتابخانه‌ها
pip3 install -r requirements.txt

# تنظیم محیط
export PYTHONIOENCODING=utf-8
export LANG=C.UTF-8
export LC_ALL=C.UTF-8

# اجرا
python3 project_fetcher.py
```

## استفاده

برنامه به صورت خودکار:

1. به API کارلنسر متصل می‌شود
2. پروژه‌های جدید را جستجو می‌کند
3. پروژه‌های تکراری را با cache تشخیص می‌دهد
4. هر پروژه را در دو فرمت ذخیره می‌کند:
   - **JSON کامل**: `new_projects/project_[ID]_[TITLE].json`
   - **متن فرمت‌شده**: `claude_input/project_[ID].txt`

## ساختار فایل‌ها

```
.
├── project_fetcher.py       # برنامه اصلی
├── run_fetcher.sh           # اسکریپت راه‌انداز
├── requirements.txt         # وابستگی‌ها
├── seen_projects.json       # Cache پروژه‌های دیده شده
├── new_projects/            # فایل‌های JSON پروژه‌ها
└── claude_input/            # فایل‌های متنی برای Claude
```

## تنظیمات پیشرفته

### تغییر کلمه کلیدی جستجو

فایل `project_fetcher.py` را ویرایش کنید:

```python
# خط آخر تابع main()
new_projects = fetcher.get_new_projects("python")  # تغییر دهید
```

### افزودن headers سفارشی

در متد `__init__` کلاس `ProjectFetcher`:

```python
self.headers = {
    'accept': 'application/json, text/plain, */*',
    'accept-language': 'fa-IR,fa;q=0.9',  # تغییر زبان
    # ...
}
```

## عیب‌یابی

### اگر باز هم فارسی درست نمایش داده نمی‌شود:

1. **بررسی locale سیستم:**
```bash
locale -a | grep -i utf
```

2. **نصب locale فارسی:**
```bash
sudo locale-gen fa_IR.UTF-8
sudo update-locale
```

3. **بررسی terminal:**
```bash
echo $LANG
# باید C.UTF-8 یا fa_IR.UTF-8 باشد
```

4. **اجرا با تنظیمات صریح:**
```bash
PYTHONIOENCODING=utf-8 LANG=C.UTF-8 python3 project_fetcher.py
```

### اگر خطای connection داد:

- اتصال اینترنت را بررسی کنید
- Bearer token را بررسی کنید
- firewall یا proxy را بررسی کنید

### اگر فایل‌ها خالی هستند:

- دسترسی نوشتن در پوشه را بررسی کنید:
```bash
ls -la
chmod +w .
```

## تفاوت‌های کلیدی با نسخه قبلی

| ویژگی | نسخه قبلی | نسخه جدید |
|-------|-----------|-----------|
| Locale | ❌ تنظیم نمی‌شد | ✅ خودکار تنظیم می‌شود |
| stdout encoding | ❌ پیش‌فرض سیستم | ✅ اجباراً UTF-8 |
| Error handling | ⚠️ ساده | ✅ پیشرفته با لاگ فارسی |
| Environment vars | ❌ تنظیم نمی‌شد | ✅ خودکار تنظیم می‌شود |
| File operations | ⚠️ گاهی encoding نداشت | ✅ همیشه UTF-8 صریح |
| Retry logic | ❌ نداشت | ✅ 3 بار تلاش مجدد |
| Logging | ⚠️ ساده | ✅ با متدهای اختصاصی |

## مثال خروجی

```
==============================================================
🔍 جستجوی پروژه‌های جدید در کارلنسر...
==============================================================

✅ دریافت 10 پروژه از API
✅ تعداد 3 پروژه جدید پیدا شد!

────────────────────────────────────────────────────────────
📋 پروژه #1: توسعه ربات تلگرام با Python
💰 بودجه: 5,000,000 - 10,000,000 تومان
⏱️  مدت زمان: 14 روز
💾 فایل JSON: new_projects/project_12345_توسعه_ربات_تلگرام.json
📝 فایل متنی: claude_input/project_12345.txt

────────────────────────────────────────────────────────────
✅ پروژه جدیدی یافت نشد

==============================================================
✅ پایان عملیات
==============================================================
```

## لایسنس

MIT

## پشتیبانی

در صورت بروز مشکل، لاگ‌ها را با دستور زیر ذخیره و ارسال کنید:

```bash
./run_fetcher.sh 2>&1 | tee fetcher.log
```

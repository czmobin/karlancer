# 🔍 راهنمای فیلترینگ و عیب‌یابی

## ❓ چرا هیچ پروژه‌ای ارسال نمیشه؟

اگه ربات هیچ پروژه‌ای رو ارسال نمیکنه، احتمالا یکی از این دلایل رو داره:

### 1️⃣ حداقل امتیاز ستاره خیلی بالاست

**پیش‌فرض**: `--min-stars 3` (فقط پروژه‌های 3 ستاره یا بالاتر)

اگه Claude به خیلی از پروژه‌ها امتیاز پایین میده، این رو امتحان کن:

```bash
# کاهش حداقل امتیاز به 2 ستاره
python3 continuous_karlancer.py --auto-submit --min-stars 2

# کاهش حداقل امتیاز به 1 ستاره (توصیه نمیشه!)
python3 continuous_karlancer.py --auto-submit --min-stars 1
```

### 2️⃣ حالت Strict فعاله

اگه از `--strict` استفاده کردی، فقط پروژه‌های Python/Django/Telegram قبول میشن.

**راه حل**: `--strict` رو حذف کن:

```bash
# بدون strict (توصیه میشه)
python3 continuous_karlancer.py --auto-submit --min-stars 3

# با strict (خیلی سخت‌گیرانه)
python3 continuous_karlancer.py --auto-submit --min-stars 3 --strict
```

### 3️⃣ بودجه پروژه‌ها کمتر از 1.5M است

ربات پروژه‌های زیر 1.5 میلیون تومان رو رد میکنه.

**راه حل**: اگه میخوای این فیلتر رو غیرفعال کنی، باید کد رو ویرایش کنی:

فایل: `continuous_karlancer.py` - خط 380

```python
# تغییر بده از:
if min_budget < 1_500_000:

# به:
if min_budget < 500_000:  # یا هر عدد دیگه‌ای
```

### 4️⃣ Claude نمیتونه پروژه رو پارس کنه

اگه Claude فرمت متفاوتی برای تحلیل استفاده کنه، ممکنه امتیاز ستاره پیدا نشه.

**راه حل**: تست کن که آیا ستاره‌ها پیدا میشن:

```bash
# تست یک فایل تحلیل
python3 test_filter.py proposals/project_12345_analysis.txt
```

### 5️⃣ Claude توصیه "Skip" داده

اگه Claude بگه "رد کن" یا "Skip"، پروژه ارسال نمیشه.

**راه حل**: بررسی فایل‌های تحلیل تو `proposals/` و ببین چرا رد شدن.

---

## 🧪 تست و عیب‌یابی

### تست فیلترها

```bash
# تست فیلتر تکنولوژی
python3 test_filter.py
```

این اسکریپت نشون میده:
- کدوم پروژه‌ها قبول میشن
- کدوم تکنولوژی‌ها blacklist هستن
- آیا parser ستاره‌ها کار میکنه

### تست یک فایل تحلیل واقعی

```bash
python3 test_filter.py proposals/project_12345_analysis.txt
```

این نشون میده:
- چند ستاره پیدا شد
- تصمیم Claude چی بود (Take/Skip)
- بخش توصیه کجاست

### بررسی لاگ‌ها

```bash
# آخرین لاگ‌ها
tail -f continuous_bot.log

# جستجو برای پروژه‌های رد شده
grep "رد شد" continuous_bot.log
grep "Rejected" continuous_bot.log

# جستجو برای پروژه‌های ارسال شده
grep "ارسال شد" continuous_bot.log
grep "Submitted" continuous_bot.log
```

### بررسی آمار

```bash
# نمایش آمار کلی
cat continuous_tracking.json | python3 -m json.tool | head -20

# نمایش پروژه‌های رد شده
cat continuous_tracking.json | python3 -m json.tool | grep "Rejected" -A 2 -B 2
```

---

## ⚙️ تنظیمات توصیه شده

### 🟢 برای شروع (ملایم):

```bash
python3 continuous_karlancer.py --auto-submit --min-stars 2
```

این تنظیمات:
- ✅ فقط WordPress, Shopify, Frontend رو رد میکنه (blacklist)
- ✅ هر پروژه‌ای که 2 ستاره یا بالاتر داره رو قبول میکنه
- ✅ بودجه حداقل 1.5M
- ⚠️ ممکنه پروژه‌های کمی نامرتبط هم ارسال بشه

### 🟡 توصیه شده (متوسط):

```bash
python3 continuous_karlancer.py --auto-submit --min-stars 3
```

این تنظیمات:
- ✅ فقط blacklist چک میشه
- ✅ فقط پروژه‌های 3 ستاره به بالا
- ✅ بودجه حداقل 1.5M
- ✅ تعادل خوب بین کیفیت و تعداد

### 🔴 سخت‌گیرانه (strict):

```bash
python3 continuous_karlancer.py --auto-submit --min-stars 4 --strict
```

این تنظیمات:
- ✅ blacklist فعال
- ✅ whitelist فعال (فقط Python/Django/Telegram/Backend)
- ✅ فقط پروژه‌های 4 ستاره به بالا
- ✅ بودجه حداقل 1.5M
- ⚠️ ممکنه خیلی کم پروژه قبول بشه

---

## 📊 فهم نتایج

### نمونه خروجی موفق:

```
[2024-01-05 10:30:15] ℹ️  [1/5] پروژه 12345: ساخت ربات تلگرام
[2024-01-05 10:30:20] ✅ تحلیل پروژه 12345 موفق (1250 chars)
[2024-01-05 10:30:21] 📊 امتیاز توصیه: ⭐⭐⭐⭐ (4/5) - تصمیم: Take
[2024-01-05 10:30:21] ✅ پروژه واجد شرایط ارسال است!
[2024-01-05 10:30:22] 📤 ارسال خودکار proposal برای پروژه 12345...
[2024-01-05 10:30:25] ✅ ✅ پروژه 12345 با موفقیت ارسال شد!
```

### نمونه خروجی رد شده (تکنولوژی):

```
[2024-01-05 10:30:15] ℹ️  [2/5] پروژه 12346: طراحی سایت وردپرس
[2024-01-05 10:30:20] ✅ تحلیل پروژه 12346 موفق (980 chars)
[2024-01-05 10:30:21] ⚠️  تکنولوژی نامناسب پیدا شد: wordpress
[2024-01-05 10:30:21] ❌ پروژه 12346 رد شد: Contains blacklisted tech: wordpress
[2024-01-05 10:30:21] 🚫 پروژه 12346 برای ارسال تایید نشد: Contains blacklisted tech: wordpress
```

### نمونه خروجی رد شده (امتیاز کم):

```
[2024-01-05 10:30:15] ℹ️  [3/5] پروژه 12347: پروژه ساده Python
[2024-01-05 10:30:20] ✅ تحلیل پروژه 12347 موفق (1100 chars)
[2024-01-05 10:30:21] 📊 امتیاز توصیه: ⭐⭐ (2/5) - تصمیم: Skip
[2024-01-05 10:30:21] ❌ امتیاز کم (2 < 3) - رد می‌شود
[2024-01-05 10:30:21] 🚫 پروژه 12347 برای ارسال تایید نشد: Rating too low: 2 stars
```

---

## 🔧 سفارشی‌سازی فیلترها

### تغییر Blacklist

فایل: `continuous_karlancer.py` - خط 48

```python
self.tech_blacklist = [
    'wordpress', 'wp', 'woocommerce',
    'shopify',
    # میتونی اینا رو اضافه یا حذف کنی
]
```

### تغییر Whitelist (فقط در حالت --strict)

فایل: `continuous_karlancer.py` - خط 63

```python
self.tech_whitelist = [
    'python', 'django', 'fastapi', 'flask',
    'telegram', 'bot', 'ربات',
    # میتونی اضافه کنی
]
```

### تغییر حداقل بودجه

فایل: `continuous_karlancer.py` - خط 380

```python
if min_budget < 1_500_000:  # تغییر بده به مقدار دلخواه
```

---

## 💡 نکات مهم

1. **شروع با تنظیمات ملایم**: اول با `--min-stars 2` شروع کن تا ببینی چقدر پروژه قبول میشه

2. **بررسی لاگ‌ها**: همیشه `continuous_bot.log` رو چک کن ببینی چرا پروژه‌ها رد میشن

3. **استفاده از --once برای تست**: قبل از اجرای مداوم، یکبار با `--once` تست کن

4. **بررسی آمار**: فایل `continuous_tracking.json` همه اطلاعات رو داره

5. **صبور باش**: ممکنه تو چند ساعت اول هیچ پروژه مناسبی نیاد

---

## 🆘 کمک بیشتر

اگه هنوز مشکل حل نشد:

1. یک فایل تحلیل نمونه رو بررسی کن: `cat proposals/project_*_analysis.txt`
2. اسکریپت تست رو اجرا کن: `python3 test_filter.py`
3. لاگ‌های آخر رو بخون: `tail -100 continuous_bot.log`
4. آمار رو چک کن: `cat continuous_tracking.json`

با این اطلاعات میتونی بفهمی دقیقا چی داره اتفاق میفته!

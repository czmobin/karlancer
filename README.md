# 🤖 Karlancer Automation Bot

ربات کاملاً خودکار برای پلتفرم کارلنسر که به صورت مداوم پروژه‌های جدید را پیدا، تحلیل و ارسال می‌کند.

## 🚀 شروع سریع (30 ثانیه)

### گزینه ۱: بدون ارسال خودکار (فقط تحلیل)

```bash
# دریافت و تحلیل پروژه‌ها (بدون ارسال)
bash run_continuous_bot.sh
```

### گزینه ۲: با ارسال خودکار ⚠️

```bash
# ارسال خودکار بدون تایید!
bash auto_karlancer.sh
```

همین! ✅

---

## 📚 مستندات کامل

### برای کاربران عادی

- **[QUICK_START_CONTINUOUS.md](QUICK_START_CONTINUOUS.md)** - شروع 30 ثانیه‌ای
- **[CONTINUOUS_BOT.md](CONTINUOUS_BOT.md)** - راهنمای کامل ربات مداوم
- **[AUTO_SUBMIT_GUIDE.md](AUTO_SUBMIT_GUIDE.md)** - راهنمای ارسال خودکار ⚠️

### برای توسعه‌دهندگان

- **[README_FETCHER.md](README_FETCHER.md)** - دریافت پروژه‌ها
- **[ANALYZER_FIX.md](ANALYZER_FIX.md)** - تحلیلگر
- **[SUBMIT_PROPOSAL_FIX.md](SUBMIT_PROPOSAL_FIX.md)** - ارسال proposal
- **[EXTRACT_PROPOSAL_FIX.md](EXTRACT_PROPOSAL_FIX.md)** - استخراج proposal
- **[CHANGES.md](CHANGES.md)** - تغییرات encoding

### عیب‌یابی

- **[quick_start.md](quick_start.md)** - راهنمای سریع analyzer
- **[debug_analyzer.sh](debug_analyzer.sh)** - ابزار عیب‌یابی

---

## 🎯 حالت‌های مختلف

| حالت | دستور | توضیحات |
|------|-------|---------|
| **فقط تحلیل** | `bash run_continuous_bot.sh` | بدون ارسال خودکار |
| **تحلیل + ارسال دستی** | `bash auto_submit.sh` | می‌پرسه قبل از ارسال |
| **ارسال خودکار** ⚠️ | `bash auto_karlancer.sh` | خودکار بدون تایید! |

---

## 📁 ساختار فایل‌ها

```
karlancer/
├── 🔧 اسکریپت‌های اصلی
│   ├── project_fetcher.py          # دریافت پروژه‌ها
│   ├── continuous_karlancer.py     # ربات مداوم
│   ├── submit_proposal.py          # ارسال proposal
│   ├── analyze_fixed.sh            # تحلیلگر
│   └── debug_analyzer.sh           # عیب‌یابی
│
├── 🚀 راه‌اندازها
│   ├── run_fetcher.sh              # دریافت پروژه‌ها
│   ├── run_continuous_bot.sh       # ربات مداوم (بدون ارسال)
│   ├── auto_submit.sh              # ارسال دستی
│   └── auto_karlancer.sh           # ارسال خودکار ⚠️
│
├── ⚙️  تنظیمات
│   ├── karelancer_prompt.txt       # System prompt برای Claude
│   ├── requirements.txt            # وابستگی‌ها
│   └── .gitignore                  # Git ignore
│
├── 📝 داده‌ها (Git ignored)
│   ├── claude_input/               # پروژه‌های دریافتی
│   ├── proposals/                  # تحلیل‌ها
│   ├── seen_projects.json          # Cache
│   └── continuous_tracking.json    # آمار
│
└── 📚 مستندات
    ├── README.md                   # این فایل
    ├── CONTINUOUS_BOT.md           # راهنمای ربات
    ├── AUTO_SUBMIT_GUIDE.md        # راهنمای auto-submit
    └── ...
```

---

## 💡 Workflows رایج

### Workflow ۱: تست اولیه

```bash
# ۱. عیب‌یابی
bash debug_analyzer.sh

# ۲. دریافت پروژه‌ها
python3 project_fetcher.py

# ۳. تحلیل
bash analyze_fixed.sh

# ۴. مشاهده نتایج
ls -lh proposals/
cat proposals/project_*_analysis.txt
```

### Workflow ۲: اجرای روزانه (دستی)

```bash
# یک بار در روز
bash auto_submit.sh

# بررسی proposal ها
# تایید یا رد کردن هر کدام
```

### Workflow ۳: ربات مداوم (بدون ارسال)

```bash
# شروع
bash run_continuous_bot.sh

# در background
nohup bash run_continuous_bot.sh > bot.log 2>&1 &
```

### Workflow ۴: کاملاً خودکار ⚠️

```bash
# ⚠️  فقط اگر کاملاً مطمئن هستید!
bash auto_karlancer.sh

# یا با systemd
sudo systemctl start auto-karlancer
```

---

## 🔧 نصب و پیش‌نیازها

### پیش‌نیازها

1. **Python 3.8+**
   ```bash
   python3 --version
   ```

2. **Claude CLI**
   ```bash
   which claude
   ```

3. **jq** (برای پردازش JSON)
   ```bash
   sudo apt-get install jq  # Ubuntu/Debian
   sudo yum install jq      # CentOS/RHEL
   ```

### نصب

```bash
# ۱. Clone repository
git clone <repo-url>
cd karlancer

# ۲. نصب وابستگی‌ها
pip3 install -r requirements.txt

# ۳. تنظیم Bearer Token
# ویرایش فایل‌های Python و token خود را قرار دهید

# ۴. اجرا
bash run_continuous_bot.sh
```

---

## 📊 مثال خروجی

```
🤖 Continuous Karlancer Bot
================================================================================

🚀 ربات مداوم کارلنسر شروع شد
⏰ فاصله بررسی: 300 ثانیه (5 دقیقه)
📤 ارسال خودکار: غیرفعال
================================================================================

🔄 چرخه #1 - 2024-12-23 20:00:00
ℹ️  جستجوی پروژه‌های جدید...
✅ 🆕 3 پروژه جدید پیدا شد!

================================================================================
ℹ️  [1/3] پروژه 257000: توسعه ربات تلگرام
================================================================================
ℹ️  تحلیل پروژه 257000 با Claude...
✅ تحلیل پروژه 257000 موفق (2456 chars)
ℹ️  ارسال خودکار غیرفعال است - پروژه 257000 آماده ارسال دستی

...

✅ پردازش 3 پروژه تمام شد
ℹ️  📊 آمار کل: 3 دریافت، 3 تحلیل، 0 ارسال، 0 خطا

😴 استراحت 300 ثانیه تا چرخه بعدی...
```

---

## 🆘 پشتیبانی

### مشکلات رایج

**۱. "Claude CLI not found"**
```bash
# نصب Claude CLI
# از https://claude.ai/download
```

**۲. "متن فارسی خراب نمایش داده می‌شود"**
```bash
# تنظیم locale
export LANG=C.UTF-8
export LC_ALL=C.UTF-8
```

**۳. "Could not extract proposal"**
```bash
# بررسی prompt
cat karelancer_prompt.txt | tail -20
```

**۴. "بودجه کمتر از حداقل پروژه"**
```bash
# این خطا حل شده - مطمئن شوید از آخرین نسخه استفاده می‌کنید
git pull
```

### عیب‌یابی کامل

```bash
# ۱. اجرای ابزار debug
bash debug_analyzer.sh

# ۲. بررسی لاگ‌ها
tail -f continuous_bot.log
cat analyzer.log

# ۳. تست encoding
python3 test_encoding.py
```

---

## ⚙️ تنظیمات پیشرفته

### تغییر فاصله زمانی

```bash
# در run_continuous_bot.sh
bash run_continuous_bot.sh 120  # هر 2 دقیقه

# در auto_karlancer.sh
bash auto_karlancer.sh 600  # هر 10 دقیقه
```

### Systemd Service

```bash
# نصب
sudo cp auto-karlancer.service /etc/systemd/system/
sudo systemctl daemon-reload
sudo systemctl enable auto-karlancer
sudo systemctl start auto-karlancer

# بررسی
sudo systemctl status auto-karlancer
```

---

## 📈 آمار و Monitoring

```bash
# تعداد کل پروژه‌های پردازش شده
cat continuous_tracking.json | jq '.total_fetched'

# تعداد ارسال‌های موفق
cat continuous_tracking.json | jq '.total_submitted'

# لیست پروژه‌های اخیر
ls -lht proposals/ | head -10

# مشاهده لاگ زنده
tail -f continuous_bot.log
```

---

## 🔐 امنیت

### توصیه‌ها

- Bearer token را محرمانه نگه دارید
- از VPN استفاده کنید (در سرور خارجی)
- لاگ‌ها را محافظت کنید: `chmod 600 *.log`
- فایل‌های Python را محافظت کنید: `chmod 600 *.py`

---

## 📝 Changelog

### نسخه فعلی

- ✅ پشتیبانی کامل UTF-8 برای لینوکس
- ✅ ربات مداوم با real-time processing
- ✅ Auto-detect بودجه از API
- ✅ ارسال خودکار proposal
- ✅ Error recovery و retry logic
- ✅ Logging و tracking کامل

---

## 🤝 مشارکت

این یک پروژه شخصی است. برای پیشنهادات یا گزارش باگ، Issue ایجاد کنید.

---

## ⚖️ مسئولیت

**⚠️ هشدار:** استفاده از حالت auto-submit کاملاً به مسئولیت خودتان است. مطمئن شوید که:

- Claude به درستی تنظیم شده
- Prompt صحیح است
- Token معتبر است
- تست کامل انجام داده‌اید

---

## 📞 تماس

برای سوالات یا مشکلات، Issue ایجاد کنید یا مستندات را مطالعه کنید.

---

**ساخته شده با ❤️ برای خودکارسازی کارلنسر**
